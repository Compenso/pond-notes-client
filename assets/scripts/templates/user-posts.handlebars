{{#each posts as |post|}}
  {{log post}}
  <section data-id="{{post.id}}">
    <h6>Title:</h6>
    <h3>
      {{limit post.title 15}}
    </h3>
    <p>
      {{#if post.content}}
        {{post.content}}
      {{else}}
        No Post Content.
        {{/if}}
      </p>
    </section>

  {{/each}}
